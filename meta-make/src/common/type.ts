
export type DataInfo = {
  header: string[],
  data: string[][],
  rowCount: number
}

export type KBInfo = {
  id: string,
  name: string,
  format: string,
  changedOn: Date,
  path: string
}

export type MetaFormat = {

}
